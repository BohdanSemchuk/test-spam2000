apiVersion: v1
kind: Service
metadata:
  name: {{ include "spam-test.fullname" . }}
  labels:
    {{- include "spam-test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spam-test.selectorLabels" . | nindent 4 }}
