apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spam-test.fullname" . }}-test-connection"
  labels:
    {{- include "spam-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spam-test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
